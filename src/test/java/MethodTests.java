import org.junit.Test;

public class MethodTests {
    /*
    Ways we can do find and replace:
        - Find all substrings in the text area and if a substring == the string we are finding then replace
        - Split the text area into words and if the words == string we are finding then replace
     Outcome:
        - Do neither of these... String has a replace() method which we can call on the entire text area
     */

    /*
        Thought:
                Run test from a "Test" button in the text editor's menu
                Or only test things that don't rely on a textEditor fields
                Or call TE from the test class
     */

//    @Test
//    public void testGetNumOccurrences() {
//        String input = "kfdsndfTHIS1sdjjfdjTHIS2kdsfsdjoTHIS3sdfhkhfdkTHIS4";
//        String substring = "tHiS";
//        int expected = 4;
//        int actual = countOccurrencesInString(substring, input);
//    }
}
